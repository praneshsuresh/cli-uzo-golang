# CLI tool to unzip and open files in it

- Uses Golang 1.16 to build a Command Line Interface tool
- The tool helps unzip and open all files in that zip folder in a single command
- It allows to open the zip folder/files in a text editor like VS Code

## Usage